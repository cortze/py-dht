import unittest
from dht.network import Network

class TestNetwork(unittest.TestCase):

    def test_network(self):
        # configuration of the network
        networkSize = 200
        nodeIDs = range(1, networkSize+1, 1)
        nodes 

